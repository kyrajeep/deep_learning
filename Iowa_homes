# This set of code is from Kaggle Learn. 
import pandas as pd

main_file_path = '../input/train.csv' # this is the path to the Iowa data that you will use
data = pd.read_csv(main_file_path)

# The cod below will help you see how output appears when you run a code block
# Run this code block with the control-enter keys on your keyboard. Or click the blue botton on the left
data.describe()
two_columns = data[['LotArea', 'OverallQual']]
two_columns.describe()

from sklearn.tree import DecisionTreeRegressor
from sklearn.model_selection import train_test_split

train_x,train_y,test_x,test_y = train_test_split(X, y, random state = 0)
iowa_decision_tree = DecisionTreeRegressor()
iowa_decision_tree.fit(X,y)
predictions = iowa_decision_tree.predict(test_x)
error = mean_absolute_error(test_y,predictions)
print(error)


